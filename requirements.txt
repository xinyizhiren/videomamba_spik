# Install the PyTorch build that matches the server CUDA version first when needed.
torch
numpy
decord
einops
timm
tqdm
torchinfo

# Required by VideoMamba/Mamba kernels in normal training environments.
mamba-ssm
causal-conv1d

# Required by the active LIF SNN layer.
spikingjelly
